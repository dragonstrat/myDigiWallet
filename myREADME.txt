How to run
********************

The development and test platform is linux (Ubuntu 14.04).
I included Makefile for the convinience of compile.

To compile, type: make
by running run.sh on linux platform, we can run the compiled code.

